(function () {
    $(".panzoom").panzoom({
        minScale: 0,
        $zoomRange: $(".zoom-range"),
        $zoomIn: $(".zoom-in"),
        $zoomOut:$(".zoom-out"),
        $reset: $(".reset")
    });
})();
